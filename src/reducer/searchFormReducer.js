const initialState = {
    loading: false,
    errors: null  
};

const searchFormReducer = (state = initialState, action) => {
    switch (action.type) {
        case "":
            
    
        default:
            return state;
    }
}